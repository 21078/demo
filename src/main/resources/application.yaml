server:
  port: 8080
  servlet:
    context-path: /

spring:
  # ai配置
  ai:
    dashscope:
      api-key: sk-b917f97befbb4ed894b822f29d81220f
    # mcp:
    #   client:
    #     enabled: true
    #     name: pdftool
    #     version: 1.0.0


        # sse:这个也难搞
        #   connections:
        #     server1:
        #       url:http://localhost:8081 #mcp服务 
        # stdio:不知道为什么不行
        #   # 关键：使用外部 JSON 文件配置
        #   servers-configuration: classpath:mcp-servers.json
  
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pdftool?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
  
  # # Redis配置
  # data:
  #   redis:
  #     host: 127.0.0.1
  #     port: 6379
  #     lettuce:
  #       pool:
  #         max-active: 10
  #         max-idle: 10
  #         min-idle: 1
  #         time-between-eviction-runs: 10s

# MyBatis-Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

# Knife4j配置
knife4j:
  enable: true